import React from 'react';
import { Container } from './styles';

const Home: React.FC = () => {
  return (
    <Container>
      <h1>Selecione uma tabela no menu lateral</h1>
    </Container>
  );
};

export default Home;
